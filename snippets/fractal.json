{
  "fratal function snippet": {
    "prefix": "ffs",
    "body": [
      "const ${1:name} = async (${2:input}) => {",
      "  const payload = { success: false, data: null, message: '', error: null };",
      "  try {",
      "    $0",
      "",
      "    payload.success = true;",
      "    payload.data = data;",
      "    return payload;",
      "  } catch (error) {",
      "    payload.success = false;",
      "    payload.data = null;",
      "    payload.error = error",
      "    return payload;",
      "  }",
      "};"
    ],
    "description": "Create a Function with Fractal Pattern"
  }
}
